# README TEMPLATE

```
config.toml |- Makefile (local/docker?)
     |            |
     v            v
settings.py <-- .env --> docker-compose.yaml
          \              /
           \            /
            \          /
        🚀 db connection 🚀
```
**All** you need to do is fill out `config.toml` and run the command from the `Makefile`.
```commandline
make help
```

To be filled out soon...
